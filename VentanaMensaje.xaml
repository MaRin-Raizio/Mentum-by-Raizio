<Window
    x:Class="MentumLauncher.VentanaMensaje"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Mentum"
    Width="520" Height="Auto"
    MinHeight="200"
    SizeToContent="Height"
    Background="#0F1117"
    FontFamily="Segoe UI"
    WindowStyle="None"
    AllowsTransparency="False"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen">

    <Window.Resources>

        <Style x:Key="BtnBase" TargetType="Button">
            <Setter Property="Height"          Value="36"/>
            <Setter Property="MinWidth"        Value="100"/>
            <Setter Property="Padding"         Value="16,0"/>
            <Setter Property="FontFamily"      Value="Segoe UI"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.80"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.60"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnGreen" BasedOn="{StaticResource BtnBase}" TargetType="Button">
            <Setter Property="Background"  Value="#0D2318"/>
            <Setter Property="BorderBrush" Value="#166534"/>
            <Setter Property="Foreground"  Value="#86EFAC"/>
        </Style>

        <Style x:Key="BtnRed" BasedOn="{StaticResource BtnBase}" TargetType="Button">
            <Setter Property="Background"  Value="#200F0F"/>
            <Setter Property="BorderBrush" Value="#7F1D1D"/>
            <Setter Property="Foreground"  Value="#FCA5A5"/>
        </Style>

        <Style x:Key="BtnNeutral" BasedOn="{StaticResource BtnBase}" TargetType="Button">
            <Setter Property="Background"  Value="#181B28"/>
            <Setter Property="BorderBrush" Value="#252836"/>
            <Setter Property="Foreground"  Value="#9CA3AF"/>
        </Style>

    </Window.Resources>

    <Border BorderBrush="#252836" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="1"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Barra de título -->
            <Grid Grid.Row="0" Background="#0D2318" MouseDown="TitleBar_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal"
                            VerticalAlignment="Center" Margin="14,0,0,0">
                    <Border Width="26" Height="26"
                            Background="#1E1530" BorderBrush="#7C3AED"
                            BorderThickness="1" CornerRadius="6" Margin="0,0,10,0">
                        <TextBlock x:Name="IconText" Text="ℹ" FontSize="13"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="#A78BFA"/>
                    </Border>
                    <TextBlock x:Name="TitleText"
                               VerticalAlignment="Center"
                               FontSize="13" FontWeight="SemiBold"
                               Foreground="#86EFAC"
                               Text="Mentum Launcher"/>
                </StackPanel>

                <!-- Botón cerrar -->
                <Button Grid.Column="2"
                        Width="36" Height="28"
                        Background="Transparent" BorderThickness="0"
                        Margin="0,0,8,0"
                        Cursor="Hand"
                        Click="BtnClose_Click"
                        Foreground="#FCA5A5"
                        FontSize="13" FontFamily="Consolas">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" Background="Transparent" CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="#200F0F"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <TextBlock Text="✕" Foreground="#FCA5A5" FontFamily="Consolas" FontSize="13"/>
                </Button>
            </Grid>

            <Rectangle Grid.Row="1" Height="1" Fill="#166534"/>

            <!-- Contenido -->
            <StackPanel Grid.Row="2" Margin="24,20,24,20">

                <!-- Ícono grande + mensaje -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="48"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Width="40" Height="40"
                            VerticalAlignment="Top"
                            Background="#1E1530" BorderBrush="#7C3AED"
                            BorderThickness="1" CornerRadius="10">
                        <TextBlock x:Name="BigIcon" Text="ℹ"
                                   FontSize="20"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="#A78BFA"/>
                    </Border>

                    <TextBlock x:Name="MessageText"
                               Grid.Column="1"
                               Margin="12,0,0,0"
                               FontSize="13"
                               Foreground="#D1D5DB"
                               TextWrapping="Wrap"
                               LineHeight="22"
                               Text=""/>
                </Grid>

                <!-- Botones -->
                <StackPanel x:Name="ButtonPanel"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"/>

            </StackPanel>
        </Grid>
    </Border>
</Window>
